[package]
edition      = "2024"
name         = "esp"
rust-version = "1.88"
version      = "0.1.0"

[[bin]]
name = "esp"
path = "./src/bin/main.rs"

[dependencies]
esp-hal = { version = "~1.1.0", features = ["esp32", "log-04", "unstable"] }


esp-bootloader-esp-idf = { version = "0.5.0", features = ["esp32", "log-04"] }
log                    = "0.4.27"

critical-section = "1.2.0"
esp-backtrace = { version = "0.19.0", features = [
  "esp32",
  "panic-handler",
  "println",
] }
esp-println = { version = "0.17.0", features = ["esp32", "log-04"] }

esp-alloc = "0.10.0"
esp-rtos = { version = "0.3.0", features = ["esp32", "embassy", "esp-radio", "log-04"] }
esp-radio = { version = "0.18.0", features = ["esp32", "log-04", "unstable", "wifi"] }

embassy-executor = "0.10.0"
embassy-net = { version = "0.8.0", features = ["dhcpv4", "medium-ethernet", "tcp"] }
static_cell = "2.1.0"
embassy-time = "0.5.1"
embedded-dht-rs = { version = "0.5.0", features = ["dht11"] }
ssd1306 = "0.10.0"
embedded-graphics = "0.8.2"

# For fine tuning these settings, please refer to https://doc.rust-lang.org/cargo/reference/profiles.html
[profile.dev]
# The default debug profile is too slow and too big for resource-constrained devices.
# Always build with some optimizations enabled.
opt-level = "s"

[profile.release]
codegen-units = 1     # LLVM can perform better optimizations using a single thread
debug         = 2     # prefer slower builds but better debugging experience
lto           = 'fat'
opt-level     = 's'
